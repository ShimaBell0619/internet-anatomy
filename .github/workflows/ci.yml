name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    uses: ShimaBell0619/web-app-foundation/.github/workflows/web-ci.yml@9061ea222e5e6bba1197b03088c6cb2c13f7e0c4
    with:
      node_version_file: .node-version
      cache_dependency_path: package-lock.json
      run_e2e: true
